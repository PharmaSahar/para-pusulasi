# T3.2 Go/No-Go Report

## Probe Command
`PYTHONPATH=. .venv-2/bin/python ops/analytics_single_channel_probe.py --channel egitim_rehberi --limit 3`

## Observed Result
- OAuth: OK
- Token scopes: present (`yt-analytics.readonly`, `youtube.readonly`)
- Analytics fetch: failed for all probed videos
- Error: `Unable to find the server at youtubeanalytics.googleapis.com`

## Decision
- Gate status: **NO-GO** for live API dependent collector rollout.

## Implication
- Continue with mock-first collector core and evaluator integration.
- Keep runtime integrations that require live API disabled.
- Re-run probe after network/API enablement issue is resolved.

## Re-open Criteria
Go decision requires all true:
1. OAuth success
2. no 403 accessNotConfigured and no DNS endpoint failures
3. at least one successful analytics report row
